package Week2.ThreadsExcersises;
/*
private static class Excersise2 {
    private int count = 0;

    // Method to increment the count, synchronized to ensure thread safety
    public synchronized void increment() {
        count++;
    }

    // Method to retrieve the current count value
    public int getCount() {
        return count;
    }

    //Forstår den ikke så godt. Mit bud ville være den ikke skulle være static og public?
}

 */